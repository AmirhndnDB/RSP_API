{
  "watch": ["src", "startup", "index.ts"],
  "ext": "ts",
  "ignore": ["node_modules", "dist", "**/*.js"],
  "exec": "npm run build && node dist/index.js",
  "delay": 1000,
  "env": {
    "NODE_ENV": "development",
    "DEBUG": "app:main"
  }
}
